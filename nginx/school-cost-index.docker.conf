# Docker: upstream uses container name (nginx_network)
# Kullanım: Bu dosyayı sites-enabled'da ana config yerine kullanın
upstream school_cost_index {
    server school-cost-index:3000;
    keepalive 64;
}
